# 💰 Учёт личных финансов

**Учёт личных финансов** — это простое настольное приложение на **Python + Tkinter** для управления своими доходами и расходами.  
Проект разработан с применением **ООП**, работы с файлами (**CSV**) и принципов версионного контроля (**Git/GitHub**).

---

## 🎯 Цель проекта

Целью проекта является создание удобного desktop-приложения для пользователей, желающих вести личный финансовый учёт.  
Программа позволяет добавлять доходы и расходы, классифицировать их по категориям, сохранять данные на диск и просматривать аналитику по категориям.

---

## 🚀 Основные возможности

- 📥 **Добавление доходов и расходов** с указанием категории, суммы и описания  
- 💾 **Автоматическое сохранение данных** в CSV-файл  
- 🔄 **Загрузка данных при запуске**  
- 📊 **Анализ расходов и доходов по категориям**  
- 🧮 **Подсчёт общей суммы по каждой категории**  
- 🖥️ **Интуитивный графический интерфейс** на базе Tkinter  
- 🔐 **Без установки — полностью автономное приложение**

---

## 🧠 Используемые технологии

| Технология | Назначение |
|-------------|-------------|
| **Python 3.10+** | Основной язык программирования |
| **Tkinter** | Создание графического интерфейса |
| **CSV (встроенная библиотека)** | Хранение данных о транзакциях |
| **ООП** | Организация кода в виде классов и объектов |
| **Git / GitHub** | Контроль версий и совместная работа над проектом |

---

## 🧩 Структура проекта
```
finance_manager/
│
├── models.py # Логика и классы данных (Transaction, Category, FinanceManager)
├── gui.py # Графический интерфейс Tkinter
├── main.py # Точка входа (запуск приложения)
├── data.csv # Хранилище данных о транзакциях
└── README.md # Описание проекта
```
🧩 Классы проекта
### 1. Category
Класс описывает категорию финансовых операций.
Используется для группировки доходов и расходов.
Атрибуты:
name — название категории (например, «Еда», «Зарплата»)
type — тип категории: "доход" или "расход"
### 2. Transaction
Класс представляет отдельную финансовую операцию.
Атрибуты:
category — объект категории (Category)
amount — сумма операции
description — описание (например, «Покупка продуктов»)
date — дата операции (устанавливается автоматически)
### 3. FinanceManager
Класс управляет всеми операциями и файлами данных.
Основные методы:
add_transaction() — добавление новой операции
save_data() — сохранение данных в CSV
load_data() — загрузка операций при запуске
analyze() — анализ и подсчёт сумм по категориям

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий
bash
git clone https://github.com/username/finance_manager.git
cd finance_manager
###  2. Убедиться, что установлен Python 3.10+
Проверить версию Python:
bash
Копировать код
python --version
### 3. Запустить приложение
bash
Копировать код
python main.py
После запуска откроется окно приложения.

## 🖥️ Графический интерфейс (Tkinter)
```
Интерфейс прост и удобен:
Поля для выбора категории, ввода суммы и описания
Кнопка «Добавить» для сохранения операции
Таблица, где отображаются все операции
Кнопка «Показать аналитику», которая выводит статистику расходов и доходов по категориям
```
